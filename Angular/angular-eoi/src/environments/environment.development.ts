import { StoreStatus } from '../app/shared/Interface/store-status';

const storeStatus: StoreStatus = {
    title: 'Store Dashboard (DEV)',
    version: '1.0.0'
};

export const environment = {
    storeStatus
};
